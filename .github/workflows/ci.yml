# SPDX-License-Identifier: MIT
# Copyright 2026 Tyler Zervas
#
# RETIRED — do not expand.
# All required gates are now in fleet-ci.yml + fleet-security.yml
# (self-hosted, HONEST_CI, DYNAMIC_POOL tiers).
# This file remains only for manual re-runs / historical context.
# See: gha-runner-ctl docs/FLEET_STANDARDS.md + HONEST_CI.md

name: CI (legacy — retired)

on:
  workflow_dispatch:

env:
  CARGO_TERM_COLOR: always
  RUST_BACKTRACE: 1

jobs:
  notice:
    name: legacy CI retired
    runs-on: [self-hosted, linux, x64, podman]
    steps:
      - run: |
          echo "Legacy multi-job CI is retired."
          echo "Use fleet-ci.yml (cargo check/test on large) and fleet-security.yml."
          echo "HONEST_CI class=SKIP_STUB detail=legacy_ci_retired"
